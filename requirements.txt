# ============================================================
# Thunder AI — Python Dependencies
# Tested on: Python 3.14.5 | Windows 11
# Install:   pip install -r requirements.txt
# ============================================================

# ── Backend (FastAPI server) ─────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.13.4          # pydantic-core==2.46.4 (auto-installed)
python-dotenv==1.0.1

# ── Database ─────────────────────────────────────────────────
sqlalchemy==2.0.41

# ── AI / LLM (Ollama client + embeddings) ────────────────────
requests==2.32.2
sentence-transformers==2.7.0
torch==2.12.1              # auto-installs with sentence-transformers
transformers==4.57.6       # auto-installs with sentence-transformers
numpy==2.4.6               # auto-installs with torch
scipy==1.18.0              # auto-installs with sentence-transformers
scikit-learn==1.9.0        # auto-installs with sentence-transformers

# ── Windows Automation ───────────────────────────────────────
psutil==5.9.8
pywin32==312

# ── Voice (TTS + microphone) ─────────────────────────────────
# NOTE: pyaudio has no Python 3.14 wheel — sounddevice replaces it
pyttsx3==2.99              # offline text-to-speech (Windows SAPI)
SpeechRecognition==3.17.0  # speech-to-text via Google API
sounddevice==0.5.5         # mic input (replaces pyaudio, works on 3.14+)

# ── Optional: Advanced voice (install separately) ────────────
# faster-whisper==1.0.2    # fully offline transcription
# piper-tts==1.2.0         # fully offline neural TTS
# openwakeword==0.6.0      # wake-word detection ("hey thunder")

# ── Optional: Volume control (Windows) ───────────────────────
# pycaw==20240210          # fine-grained volume control
# comtypes==1.4.1          # required by pycaw

# ── Optional: Vision / Emotion detection ─────────────────────
# opencv-python==4.9.0.80
# mediapipe==0.10.14
