# ============================================================
# Thunder AI — Python Dependencies
# Tested on: Python 3.14.5 | Windows 11
# Install:   py -3.14 -m pip install -r requirements.txt
# NOTE: Use Python 3.14 — all packages are pinned for 3.14
# ============================================================

# ── Backend (FastAPI server) ─────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.13.4
python-dotenv==1.0.1

# ── Database ─────────────────────────────────────────────────
sqlalchemy==2.0.41

# ── AI / LLM (Ollama client + embeddings) ────────────────────
requests==2.32.2
sentence-transformers==2.7.0
torch==2.12.1
transformers==4.57.6
numpy==2.4.6
# scipy 1.18.0 only has a wheel for Python 3.14 — auto-resolved below
scipy==1.18.0
scikit-learn==1.9.0

# ── Windows Automation ───────────────────────────────────────
psutil==5.9.8
pywin32==312

# ── Voice (TTS + microphone) ─────────────────────────────────
# pyaudio has no Python 3.14 wheel — sounddevice is the replacement
pyttsx3==2.99
SpeechRecognition==3.17.0
sounddevice==0.5.5

# ── Optional: Advanced voice (install separately if needed) ──
# faster-whisper==1.0.2
# piper-tts==1.2.0
# openwakeword==0.6.0

# ── Optional: Volume control (Windows) ───────────────────────
# pycaw==20240210
# comtypes==1.4.1

# ── Optional: Vision / Emotion detection ─────────────────────
# opencv-python==4.9.0.80
# mediapipe==0.10.14
